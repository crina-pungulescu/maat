name: cron-test

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: prove it runs
        run: echo "CRON IS ALIVE $(date -u)"
